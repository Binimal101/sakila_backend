**Films Page**
- Get films by name, actor, or genre: f(input_type, input, top_n) -> len( List[Film] ) == top_n
- View details of film given film_id
- "rent out a film": needs PMT and FILM/STORE/CUST details
    - REQUIREMENTS
        - create new rental record with return_date=NULL
        - create new payment record
    - INPUT
        - PMT input: (customer_id, staff_id, rental_id) `#we assume current data is pmt date` 
        - FILM/STORE/CUST input: (film_id, customer_id, store_id)
        - endpoint input = PMT `U` FILM/STORE/CUST
            - `(customer_id, store_id, staff_id, film_id)`
    - YIELDS
        - rental_id
- I want to view ALL customers with filtration
    - f(id?, first_name?, last_name?, offset = 0, top_n = 20) -> List[Customer]
- Add new customers
    - f(store_id, first_name, last_name, email?, Address)
    - Address is created with the format of A(...).  (note that district is equivalent to state; TODO derive location from reverse_geocoding)
    - A(street_address, address_line_2?, district, postal_code?, phone_number, city, country, isModifying?)
- Modify customer details
    - use the same endpoint that creates a customer, will utilize upserts in implementation post-validity checks
- Delete customer
    - f(customer_id)
- View customer details
    - f(customer_id) -> Customer
- View customer rental records
    - f(customer_id, store_id) -> Map[("outgoing_rentals", "past_rentals") -> List[Rental]]
- Returned a rented movie
    - f(rental_id)
